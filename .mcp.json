{
  "mcpServers": {
    "token-vault": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "VAULT_DB_PATH": "${HOME}/.token-5-0/vault.db"
      }
    }
  }
}
